import { Navbar, Container, } from "react-bootstrap";

const NavigationBar = () => {
    return (
        <div>
            <Navbar>
                <Container >
                    <Navbar.Brand className="text-white">FAHRULFIM21</Navbar.Brand>
                </Container>
            </Navbar>
        </div>
    )
}

export default NavigationBar;